import React, { MouseEventHandler } from 'react'
import { Wrapper } from './styles'


export const Upload = ({ handleSwitch }: { handleSwitch: MouseEventHandler }) => {

    return (
        <Wrapper>
        </Wrapper>
    )

}